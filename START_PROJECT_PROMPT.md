# Codex용 새 프로젝트 시작 프롬프트

아래 프롬프트 전체를 Codex 채팅에 붙여넣으세요. 이후에는 Codex의 질문에 답하기만 하면 됩니다. 터미널 명령을 입력하거나 파일을 직접 편집할 필요가 없습니다.

```text
새 프로젝트 생성을 도와줘. 나는 개발자가 아니므로, 터미널 명령을 입력하거나 파일을 직접 편집하라고 하지 말고 네가 필요한 작업을 직접 진행해줘.

템플릿 원본은 https://github.com/CVS-TaeKyoon/02.project_template 이며, 이 저장소의 boilerplate 디렉터리만 사용해.

진행 규칙:

1. 지금은 정보를 한꺼번에 묻지 마. 아래 순서대로 질문 하나를 하고 내 답을 받은 뒤에만 다음 질문을 해.
   - 먼저: "프로젝트를 만들 상위 폴더 경로를 알려주세요."
   - 다음: "새 프로젝트 이름은 무엇인가요?"
   - 마지막: "이 프로젝트는 무엇을 위한 것인가요? 쉬운 말로 설명해 주세요."

2. 세 답을 모두 받으면 `<상위 폴더 경로>\<프로젝트 이름>`이 이미 있는지 확인해. 있으면 덮어쓰거나 삭제하지 말고, 다른 이름 또는 경로를 하나만 다시 물어봐.

3. 대상 폴더가 비어 있을 때만 템플릿을 가져와. Git을 사용할 수 있으면 템플릿 저장소의 main 브랜치를 임시 위치에 받아서 boilerplate의 내용만 대상 폴더에 복사해. Git이 없으면 https://github.com/CVS-TaeKyoon/02.project_template/archive/refs/heads/main.zip 을 받아 임시 위치에 풀고, 같은 방식으로 boilerplate의 내용만 복사해. 어느 방법이든 템플릿 저장소의 .git 메타데이터는 새 프로젝트에 넣지 마.

4. 새 프로젝트 안의 모든 Markdown(.md) 파일에서 `<프로젝트 이름>`이라는 정확한 자리표시자를 프로젝트 이름으로 바꿔. README.md의 프로젝트 소개 자리와 AGENTS.md의 `## 프로젝트` 설명 자리에는 내가 말한 프로젝트 목적을 자연스러운 한두 문장으로 넣어. 목적과 관계없는 규칙·설명·자리표시자는 임의로 바꾸지 마.

5. 복사가 끝나면 생성한 폴더 경로와 바꾼 Markdown 파일 목록을 쉬운 말로 보여줘. 그다음 "이 프로젝트를 GitHub에도 올릴까요?"라고 한 번만 물어봐.

6. 내가 GitHub 업로드를 원한다고 답한 경우에만 다음을 진행해.
   - GitHub CLI(gh)가 설치되어 있는지 먼저 확인해. 설치되어 있지 않으면 GitHub CLI 공식 설치 페이지를 브라우저로 열어 안내하고, 설치가 끝날 때까지 업로드를 진행하지 마. 다른 방법으로 업로드하지 마.
   - gh가 설치되어 있으면 GitHub 로그인 상태를 확인해. 로그인이 아니면 공식 GitHub 로그인 절차를 브라우저로 열어. 비밀번호, 토큰, 인증 코드를 나에게 묻거나 대신 입력하지 마.
   - gh 설치와 로그인이 모두 확인된 뒤에만 저장소 이름을 물어봐. 기본값은 프로젝트 이름이지만, 내가 답할 때까지 결정하지 마.
   - 다음으로 공개 저장소와 비공개 저장소 중 하나를 물어봐.
   - 마지막으로 저장소 이름과 공개 여부를 요약해 보여주고, "이대로 GitHub 저장소를 만들고 첫 버전을 올릴까요?"라고 물어봐. 내가 명확히 승인하기 전에는 저장소를 만들거나 파일을 올리지 마.
   - 승인 뒤에는 .env, 키, 토큰, 비밀번호, 개인 설정 파일이 포함되지 않는지 확인한 다음에만 새 GitHub 저장소를 만들고 첫 커밋을 push해. 완료되면 저장소 링크를 알려줘.

7. 오류가 나면 기존 폴더나 파일을 삭제하지 마. 로컬 프로젝트는 그대로 보존하고, 실패 원인과 내가 선택할 수 있는 다음 행동 하나만 쉬운 말로 알려줘.

중요: 키·토큰·비밀번호·.env 내용을 채팅에 요청하거나 표시하지 마. 내가 승인하기 전에는 GitHub에 어떤 것도 만들거나 올리지 마.
```
